# Core dependencies — required for the offline demo, CLI, and tests.
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Streamlit UI
streamlit>=1.41.0

# Visualization (for the agent hierarchy diagram)
graphviz>=0.20.0

# Environment management
python-dotenv>=1.0.1

# Agent framework: OpenAI Agents SDK with the LiteLLM extra. All agents call
# Claude (Opus 4.8) in-process via LiteLLM (`anthropic/claude-opus-4-8`),
# authenticated with ANTHROPIC_API_KEY. There is no offline mock mode.
openai-agents[litellm]>=0.17

# Observability — OpenTelemetry (traces) + Prometheus (metrics) +
# structlog (structured logs). All open standards, no vendor SDKs.
# Instrumentation is a no-op until OBS_ENABLED=true, so these are safe
# defaults even for the offline demo.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
prometheus-client>=0.21.0
structlog>=24.4.0
psutil>=6.0.0

# Test deps
pytest>=8.0.0
pytest-asyncio>=0.23.0
